HashSet的使用
存储结构：哈希表（数组+链表+红黑树）
存储过程：1、根据hashcode计算保存的位置，如果此位置为空，则直接保存，如果不为空执行第二步
        2、在执行equals方法，如果equals为true，则认为是重复；否则，形成链表

TreeSet（红黑树）要求：元素必须实现Comparable接口
1、基于排列顺序实现元素不重复
2、实现了SortedSet接口，对集合元素自动排序
3、元素对象的类型必须实现Comparable接口，指定排序规则
4、通过CompareTo方法确定是否为重复元素
